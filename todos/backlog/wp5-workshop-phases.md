# WP5 — Workshop phases as agent behaviors

**Source:** docs/AI-SDLC-INTEGRATION-PLAN.md §6/WP5 · **Size:** M · **Needs:** WP2–WP4

Interviewer + SPECIFY (restate-first, forcing sections, theater refusal); PLAN (read-only
explore → editable artifact, skip affordance); EXECUTE supervised (`/next` contract via
`bench.delegate`) and gated (`/loop` contract; mode chooser asks the pass@k/pass^k
question; refuses without enabled verify checks); Reviewer via `agent.ask` + RECORD
(learnings, todo completion, skill-extraction proposal); refactor variant (coverage
precondition, characterization route). End-to-end golden scenario with independence
trajectory-asserted.
